<%= form_for(@appointmentreminder) do |f| %>
  <% if @appointmentreminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointmentreminder.errors.count, "error") %> prohibited this appointmentreminder from being saved:</h2>

      <ul>
      <% @appointmentreminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
